# Интеграция составов с формой отправки заявки

## Дата: 2026-02-08
## Ветка: feature/order-sending

## Цель

Реализовать систему сохранения составов игроков из калькулятора и автоматического заполнения формы отправки заявки (sending-form).

## Пользовательские сценарии

### Сценарий 1: Базовый (основной путь) ✅

1. Пользователь открывает страницу калькулятора (previewmatch.php)
2. Система автоматически определяет принадлежность команд
3. Пользователь подбирает оптимальный состав СВОЕЙ команды
4. Пользователь нажимает кнопку "💾 Сохранить состав"
5. Система сохраняет состав в localStorage
6. Пользователь переходит на форму отправки заявки (sending-form)
7. Пользователь нажимает кнопку "📥 Загрузить состав"
8. Система автоматически заполняет форму сохраненным составом
9. Пользователь проверяет и отправляет заявку

**Преимущества:**
- Явный контроль пользователя
- Понятный процесс
- Минимум сюрпризов

### Сценарий 2: С автосохранением

1. Пользователь открывает калькулятор
2. Система определяет принадлежность команд
3. Пользователь подбирает состав СВОЕЙ команды
4. **Система автоматически сохраняет при каждом изменении** (debounced)
5. Пользователь переходит на форму отправки
6. Пользователь нажимает "📥 Загрузить состав"
7. Система заполняет форму
8. Пользователь отправляет заявку

**Преимущества:**
- Не нужно помнить о сохранении
- Всегда актуальный состав
- Меньше кликов

**Недостатки:**
- Может сохранить незавершенный состав
- Больше операций записи в localStorage

### Сценарий 3: С автозагрузкой

1. Пользователь открывает калькулятор
2. Система определяет принадлежность команд
3. Пользователь подбирает состав и сохраняет
4. Пользователь переходит на форму отправки
5. **Система автоматически загружает сохраненный состав** (если есть)
6. Показывается уведомление: "✅ Загружен состав из калькулятора"
7. Пользователь проверяет и отправляет заявку

**Преимущества:**
- Максимально быстрый процесс
- Не нужно искать кнопку загрузки
- Seamless UX

**Недостатки:**
- Может загрузить неожиданный состав
- Нужна кнопка "Очистить" для отмены

### Сценарий 4: Просмотр чужого матча

1. Пользователь открывает калькулятор с матчем двух чужих команд
2. Система определяет, что команды не принадлежат пользователю
3. Кнопка "💾 Сохранить состав" **неактивна** с подсказкой
4. Пользователь может анализировать, но не сохранять
5. При попытке перейти на форму отправки - нет доступа

**Альтернатива:** Разрешить сохранение для анализа, но блокировать загрузку в форму отправки

### Сценарий 5: Множественные команды

1. Пользователь управляет несколькими командами
2. Открывает калькулятор для команды A
3. Сохраняет состав команды A
4. Открывает калькулятор для команды B
5. Сохраняет состав команды B
6. Переходит на форму отправки для команды A
7. Система загружает состав команды A (по teamId)
8. Переходит на форму отправки для команды B
9. Система загружает состав команды B

**Важно:** Составы хранятся отдельно по teamId

### Сценарий 6: Устаревший состав

1. Пользователь сохранил состав 10 дней назад
2. Переходит на форму отправки
3. Система проверяет возраст состава (> 7 дней)
4. Показывает предупреждение: "⚠️ Состав устарел (10 дней). Загрузить?"
5. Пользователь решает: загрузить или создать новый

**Альтернатива:** Автоматически удалять старые составы

### Сценарий 7: Недоступные игроки

1. Пользователь сохранил состав
2. Игрок получил травму/дисквалификацию
3. Пользователь загружает состав в форму
4. Система обнаруживает недоступного игрока
5. Показывает предупреждение: "⚠️ Игрок X недоступен"
6. Пользователь заменяет игрока вручную

**Требует:** API проверки доступности игроков

## Рекомендуемый подход

### Для MVP (Этап 1):
**Сценарий 1** - базовый с явным сохранением/загрузкой
- Простая реализация
- Понятное поведение
- Легко тестировать

### Для улучшений (Этап 2):
**Сценарий 2 + 3** - автосохранение + автозагрузка
- Добавить после получения обратной связи
- Сделать опциональным (настройка в UI)
- Добавить кнопку "Очистить загруженный состав"

### Для всех этапов:
**Сценарий 4** - обязательная проверка владения
**Сценарий 5** - поддержка множественных команд
**Сценарий 6** - проверка актуальности (опционально)
**Сценарий 7** - валидация игроков (опционально для Этапа 3)

## Текущая ситуация

### Калькулятор (previewmatch.php)
- ✅ Пользователь выбирает игроков в составе
- ✅ Выбирает позиции для каждого игрока
- ✅ Видит расчет силы команды
- ❌ Состав не сохраняется при переходе на другую страницу

### Форма отправки заявки (sending-form)
- ✅ Пользователь вручную выбирает игроков
- ✅ Выбирает позиции
- ❌ Нет автоматического заполнения из калькулятора
- ❌ Приходится повторно выбирать тех же игроков

## Проблема

Пользователь дважды выполняет одну и ту же работу:
1. Подбирает оптимальный состав в калькуляторе
2. Вручную переносит этот же состав в форму отправки заявки

## Требования

### Функциональные требования

1. **Сохранение состава**
   - Автоматически сохранять состав при изменениях в калькуляторе
   - Сохранять: ID игрока, позицию, индекс в составе
   - Хранить отдельно для домашней и гостевой команды

2. **Загрузка состава**
   - Автоматически загружать сохраненный состав на странице sending-form
   - Заполнять селекторы игроков и позиций
   - Показывать индикатор загрузки

3. **Управление**
   - Кнопка "Сохранить состав" в калькуляторе
   - Кнопка "Загрузить состав" в форме отправки
   - Кнопка "Очистить сохраненный состав"

4. **Валидация**
   - Проверять актуальность сохраненного состава
   - Проверять доступность игроков (не травмированы, не дисквалифицированы)
   - Показывать предупреждения при проблемах

### Нефункциональные требования

1. **Производительность**
   - Сохранение должно быть мгновенным
   - Загрузка не должна блокировать UI

2. **Надежность**
   - Использовать localStorage для хранения
   - Обрабатывать ошибки при чтении/записи
   - Не ломать существующий функционал

3. **UX**
   - Визуальная обратная связь при сохранении/загрузке
   - Понятные сообщения об ошибках
   - Не мешать обычному workflow

## Архитектура решения

### 1. Структура данных

```javascript
// Формат сохраненного состава
const savedLineup = {
    teamId: 12345,              // ID команды
    matchId: 67890,             // ID матча (опционально)
    timestamp: 1707350400000,   // Время сохранения
    formation: "4-4-2",         // Формация
    players: [
        {
            slotIndex: 0,       // Индекс позиции (0-10)
            playerId: 5490359,  // ID игрока
            position: "GK",     // Позиция
            playerName: "Иван Иванов" // Имя для отладки
        },
        // ... остальные 10 игроков
    ]
};
```

### 2. API функции

```javascript
// Сохранение состава
function saveLineupToStorage(teamId, lineup, formation) {
    const key = `vs_lineup_${teamId}`;
    const data = {
        teamId,
        timestamp: Date.now(),
        formation,
        players: lineup.map((slot, index) => ({
            slotIndex: index,
            playerId: slot.getValue(),
            position: slot.posValue,
            playerName: slot.selectedPlayer?.name || ''
        }))
    };
    localStorage.setItem(key, JSON.stringify(data));
}

// Загрузка состава
function loadLineupFromStorage(teamId) {
    const key = `vs_lineup_${teamId}`;
    const data = localStorage.getItem(key);
    if (!data) return null;
    
    try {
        const lineup = JSON.parse(data);
        // Проверка актуальности (например, не старше 7 дней)
        const age = Date.now() - lineup.timestamp;
        if (age > 7 * 24 * 60 * 60 * 1000) {
            localStorage.removeItem(key);
            return null;
        }
        return lineup;
    } catch (e) {
        console.error('[Lineup] Error loading lineup:', e);
        return null;
    }
}

// Очистка состава
function clearLineupFromStorage(teamId) {
    const key = `vs_lineup_${teamId}`;
    localStorage.removeItem(key);
}
```

### 3. Интеграция с калькулятором

**Где:** `previewmatch.php` (функция инициализации калькулятора)

```javascript
// Автосохранение при изменении состава
function setupAutoSave(teamId, lineupBlock, formationSelect) {
    // Сохранять при изменении игрока
    lineupBlock.lineup.forEach(slot => {
        slot.addEventListener('change', () => {
            saveLineupToStorage(teamId, lineupBlock.lineup, formationSelect.value);
            showSaveIndicator(); // Визуальная обратная связь
        });
    });
    
    // Сохранять при изменении формации
    formationSelect.addEventListener('change', () => {
        saveLineupToStorage(teamId, lineupBlock.lineup, formationSelect.value);
    });
}

// Кнопка ручного сохранения
function addSaveButton(container, teamId, lineupBlock, formationSelect) {
    const btn = document.createElement('button');
    btn.textContent = '💾 Сохранить состав';
    btn.className = 'butn';
    btn.onclick = () => {
        saveLineupToStorage(teamId, lineupBlock.lineup, formationSelect.value);
        alert('Состав сохранен!');
    };
    container.appendChild(btn);
}
```

### 4. Интеграция с формой отправки

**Где:** Страница `sending-form` (нужно определить точное место)

```javascript
// Автозагрузка при открытии формы
function autoLoadLineup(teamId) {
    const savedLineup = loadLineupFromStorage(teamId);
    if (!savedLineup) {
        console.log('[Lineup] No saved lineup found');
        return;
    }
    
    console.log('[Lineup] Loading saved lineup:', savedLineup);
    
    // Установить формацию
    const formationSelect = document.querySelector('#formation-select');
    if (formationSelect) {
        formationSelect.value = savedLineup.formation;
        formationSelect.dispatchEvent(new Event('change'));
    }
    
    // Заполнить игроков
    savedLineup.players.forEach(player => {
        const playerSelect = document.querySelector(`#player-${player.slotIndex}`);
        const positionSelect = document.querySelector(`#position-${player.slotIndex}`);
        
        if (playerSelect && positionSelect) {
            playerSelect.value = player.playerId;
            positionSelect.value = player.position;
            
            // Триггерим события для обновления UI
            playerSelect.dispatchEvent(new Event('change'));
            positionSelect.dispatchEvent(new Event('change'));
        }
    });
    
    showLoadIndicator(); // Визуальная обратная связь
}

// Кнопка загрузки
function addLoadButton(container, teamId) {
    const btn = document.createElement('button');
    btn.textContent = '📥 Загрузить состав из калькулятора';
    btn.className = 'butn';
    btn.onclick = () => {
        autoLoadLineup(teamId);
        alert('Состав загружен!');
    };
    container.appendChild(btn);
}
```

## План реализации

### Этап 1: Базовая функциональность (MVP)

1. **Создать API функции** ✅
   - `saveLineupToStorage()`
   - `loadLineupFromStorage()`
   - `clearLineupFromStorage()`

2. **Интегрировать с калькулятором**
   - Добавить кнопку "Сохранить состав"
   - Реализовать сохранение при клике
   - Добавить визуальную обратную связь

3. **Интегрировать с формой отправки**
   - Определить структуру формы
   - Добавить кнопку "Загрузить состав"
   - Реализовать загрузку и заполнение

4. **Тестирование**
   - Сохранение в калькуляторе
   - Загрузка в форме
   - Проверка корректности данных

### Этап 2: Улучшения

1. **Автосохранение**
   - Сохранять при каждом изменении
   - Debounce для производительности

2. **Валидация**
   - Проверка доступности игроков
   - Предупреждения о проблемах

3. **UI/UX**
   - Индикаторы загрузки
   - Анимации
   - Улучшенные сообщения

### Этап 3: Дополнительные фичи

1. **Множественные составы**
   - Сохранение нескольких вариантов
   - Именование составов
   - Выбор из списка

2. **Синхронизация**
   - Экспорт/импорт составов
   - Шаринг через URL

3. **Аналитика**
   - История изменений
   - Статистика использования

## Технические детали

### localStorage ключи

```
vs_lineup_{teamId}           - Основной состав команды
vs_lineup_{teamId}_backup    - Резервная копия
vs_lineup_meta               - Метаданные (список команд с сохраненными составами)
```

### Размер данных

Один состав: ~1-2 KB
Лимит localStorage: 5-10 MB
Можно хранить тысячи составов

### Обработка ошибок

```javascript
try {
    saveLineupToStorage(teamId, lineup, formation);
} catch (e) {
    if (e.name === 'QuotaExceededError') {
        alert('Недостаточно места для сохранения. Очистите старые составы.');
    } else {
        console.error('[Lineup] Save error:', e);
        alert('Ошибка при сохранении состава');
    }
}
```

## Вопросы для уточнения

1. **Структура формы отправки**
   - Какие селекторы используются?
   - Какие ID/классы у элементов?
   - Есть ли существующий JS код?

2. **Поведение**
   - Автосохранение или только по кнопке?
   - Автозагрузка или только по кнопке?
   - Показывать ли подтверждение?

3. **Валидация**
   - Нужна ли проверка доступности игроков?
   - Что делать с недоступными игроками?
   - Показывать ли предупреждения?

4. **Определение доступности загрузки** ⚠️ ВАЖНО
   - Как определять, какой состав можно загрузить?
   - Что делать при просмотре матча двух чужих команд?
   - Проверять ли владение командой перед загрузкой?
   - Показывать ли кнопку загрузки только для своих команд?
   - Как определить "свою" команду (по ID пользователя, по правам доступа)?

5. **UI**
   - Где разместить кнопки?
   - Какой стиль использовать?
   - Нужны ли иконки?

## Предложенные решения для определения доступности

### Проблема
При просмотре матча двух чужих команд пользователь не должен иметь возможность загрузить/сохранить состав, так как:
- Он не управляет этими командами
- Не может отправить заявку за чужую команду
- Сохранение бессмысленно

### Решение 1: Проверка владения командой

```javascript
// Определяем ID команд пользователя
function getUserTeamIds() {
    // Вариант 1: Из глобальной переменной (если есть)
    if (window.userTeamIds) {
        return window.userTeamIds;
    }
    
    // Вариант 2: Из localStorage (если сохраняется при логине)
    const userTeams = localStorage.getItem('vs_user_teams');
    if (userTeams) {
        return JSON.parse(userTeams);
    }
    
    // Вариант 3: Парсинг из меню команд на странице
    const teamLinks = document.querySelectorAll('a[href*="roster.php?num="]');
    const teamIds = Array.from(teamLinks).map(link => {
        const url = new URL(link.href, window.location.origin);
        return url.searchParams.get('num');
    });
    return [...new Set(teamIds)]; // Уникальные ID
}

// Проверка доступности команды
function isTeamAccessible(teamId) {
    const userTeams = getUserTeamIds();
    return userTeams.includes(String(teamId));
}

// Использование
function addSaveButton(container, teamId, lineupBlock, formationSelect) {
    // Проверяем доступность
    if (!isTeamAccessible(teamId)) {
        console.log('[Lineup] Team not accessible, save button hidden');
        return; // Не показываем кнопку
    }
    
    const btn = document.createElement('button');
    btn.textContent = '💾 Сохранить состав';
    // ... остальной код
}
```

### Решение 2: Визуальная индикация

Вместо полного скрытия кнопки, показывать её неактивной с подсказкой:

```javascript
function addSaveButton(container, teamId, lineupBlock, formationSelect) {
    const btn = document.createElement('button');
    btn.textContent = '💾 Сохранить состав';
    btn.className = 'butn';
    
    if (!isTeamAccessible(teamId)) {
        btn.disabled = true;
        btn.title = 'Вы не управляете этой командой';
        btn.style.opacity = '0.5';
        btn.style.cursor = 'not-allowed';
    } else {
        btn.onclick = () => {
            saveLineupToStorage(teamId, lineupBlock.lineup, formationSelect.value);
            alert('Состав сохранен!');
        };
    }
    
    container.appendChild(btn);
}
```

### Решение 3: Контекстная логика

Разное поведение в зависимости от страницы:

```javascript
function getPageContext() {
    const url = window.location.href;
    
    if (url.includes('previewmatch.php')) {
        // Страница просмотра матча - может быть чужой матч
        return {
            page: 'preview',
            requiresOwnership: false, // Можно сохранять для анализа
            canLoad: false // Но нельзя загружать в заявку
        };
    }
    
    if (url.includes('sending-form') || url.includes('sendorder')) {
        // Страница отправки заявки - только свои команды
        return {
            page: 'sending',
            requiresOwnership: true, // Обязательно проверять
            canLoad: true
        };
    }
    
    return {
        page: 'unknown',
        requiresOwnership: true,
        canLoad: false
    };
}

function shouldShowSaveButton(teamId) {
    const context = getPageContext();
    
    if (context.requiresOwnership) {
        return isTeamAccessible(teamId);
    }
    
    return true; // Показываем на страницах без ограничений
}
```

### Решение 4: Метаданные в сохраненном составе

Добавить информацию о владении в сохраненные данные:

```javascript
function saveLineupToStorage(teamId, lineup, formation) {
    const key = `vs_lineup_${teamId}`;
    const data = {
        teamId,
        timestamp: Date.now(),
        formation,
        isOwned: isTeamAccessible(teamId), // Флаг владения
        savedBy: getUserId(), // ID пользователя (если доступен)
        savedFrom: window.location.href, // Откуда сохранено
        players: lineup.map((slot, index) => ({
            slotIndex: index,
            playerId: slot.getValue(),
            position: slot.posValue,
            playerName: slot.selectedPlayer?.name || ''
        }))
    };
    localStorage.setItem(key, JSON.stringify(data));
}

function loadLineupFromStorage(teamId) {
    const lineup = loadLineupFromStorage(teamId);
    if (!lineup) return null;
    
    // Проверяем, можем ли мы использовать этот состав
    if (lineup.isOwned === false && getPageContext().requiresOwnership) {
        console.warn('[Lineup] Cannot load lineup for non-owned team');
        return null;
    }
    
    return lineup;
}
```

### Рекомендация

Использовать **комбинацию решений 1, 2 и 4**:

1. **Проверять владение** через `isTeamAccessible()`
2. **Показывать неактивную кнопку** с подсказкой для чужих команд
3. **Сохранять метаданные** о владении в составе
4. **Блокировать загрузку** чужих составов на странице отправки заявки

Это обеспечит:
- ✅ Безопасность (нельзя отправить заявку за чужую команду)
- ✅ Прозрачность (пользователь понимает, почему кнопка неактивна)
- ✅ Гибкость (можно сохранять чужие составы для анализа)
- ✅ Надежность (проверка на уровне данных)

## Связанные файлы

- `calc.user.js` - основной файл скрипта
- `docs/api/LINEUP_FUNCTIONS_API.md` - API функций составов
- `docs/enhancements/ENHANCED_LINEUP_LOADING.md` - улучшения загрузки составов

## Следующие шаги

1. Изучить структуру страницы sending-form
2. Определить селекторы для игроков и позиций
3. Реализовать MVP (Этап 1)
4. Протестировать на реальных данных
5. Собрать обратную связь
6. Реализовать улучшения (Этап 2)
