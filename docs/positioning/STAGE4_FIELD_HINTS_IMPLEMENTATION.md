# Система подсказок - ЭТАП 4: Подсказки при клике на футболки

## Статус: ✅ ЗАВЕРШЕНО

### Обзор

ЭТАП 4 добавляет интерактивные подсказки при клике на футболки игроков в поле. Это позволяет пользователям получать детальную информацию о любом игроке прямо с визуализации поля, не переключаясь к селекторам состава.

## Реализованные функции

### 1. ✅ Интерактивные футболки

#### Обновленная функция `createShirtElement()`
```javascript
function createShirtElement(position, shirtUrl, top, left, playerName, team, playerData)
```

**Новые возможности:**
- Курсор изменяется на `pointer` при наведении
- Анимация масштабирования при наведении (scale 1.1)
- Эффект свечения при наведении
- Обработчик клика для показа подсказки
- Передача данных игрока в футболку

### 2. ✅ Подсказки для игроков

#### Функция `showFieldPlayerHint()`
```javascript
function showFieldPlayerHint(position, team, playerData, shirtElement)
```

**Отображаемая информация:**
- **Базовые данные:** имя, возраст, команда, позиция
- **Силовые характеристики:** базовая и итоговая сила
- **Детальные модификаторы:**
  - Физическая форма (с цветовой индикацией)
  - Усталость (с процентным влиянием)
  - Позиционный модификатор
  - Статус реальности игрока
- **Позиционная информация:** основная, дополнительная, текущая в матче
- **Способности игрока** (если есть)

**Дизайн подсказки:**
- Современный градиентный фон
- Цветовая индикация команды (зеленый/красный)
- Анимация появления `fadeInScale`
- Адаптивное позиционирование
- Кнопка закрытия

### 3. ✅ Подсказки для пустых позиций

#### Функция `showPositionOnlyHint()`
```javascript
function showPositionOnlyHint(position, team, shirtElement)
```

**Для незаполненных позиций показывает:**
- Название позиции на русском языке
- Линию игры (защита/полузащита/атака)
- Описание роли позиции
- Информацию о том, что игрок не выбран

### 4. ✅ Расчет силы игрока

#### Функция `calculatePlayerStrength()`
```javascript
function calculatePlayerStrength(player, matchPosition, physicalFormId)
```

**Учитываемые модификаторы:**
- **Физическая форма:** от 0.95 (ужасная) до 1.05 (отличная)
- **Усталость:** от 1.0 (свежий) до 0.88 (очень уставший)
- **Позиция:** использует существующую функцию `getPositionModifier()`
- **Реальность:** 0.8 для нереальных игроков

**Возвращает:**
```javascript
{
    baseStrength: number,
    finalStrength: number,
    modifiers: {
        form: { name, coefficient, value },
        fatigue: { name, coefficient, value },
        position: { name, coefficient, value },
        reality: { name, coefficient, value }
    }
}
```

### 5. ✅ Интеграция с составами

#### Обновленная функция `displayShirtsOnField()`

**Новая логика:**
- Извлекает данные игроков из `homeLineup` и `awayLineup`
- Передает полные данные игрока в футболку
- Включает физическую форму и позицию в матче
- Поддерживает как заполненные, так и пустые позиции

## Технические детали

### Позиционирование подсказок

Функция `positionFieldHint()` обеспечивает:
- Центрирование относительно футболки
- Показ сверху с отступом 10px
- Автоматическая коррекция при выходе за границы экрана
- Показ снизу если не помещается сверху

### Информация о позициях

База данных из 18 позиций с описаниями:
- **Защита:** GK, LD, CD, RD, LB, RB, SW
- **Полузащита:** LM, CM, RM, DM, AM, FR  
- **Атака:** LW, RW, LF, RF, CF, ST

### CSS анимации

Добавлена анимация `fadeInScale`:
```css
@keyframes fadeInScale {
  0% { opacity: 0; transform: scale(0.8); }
  100% { opacity: 1; transform: scale(1); }
}
```

## Пользовательский опыт

### Взаимодействие
1. **Наведение на футболку:** масштабирование и свечение
2. **Клик на футболку:** показ детальной подсказки
3. **Клик вне подсказки:** автоматическое закрытие
4. **Кнопка "Закрыть":** ручное закрытие

### Визуальные индикаторы
- **Цвет команды:** зеленый для хозяев, красный для гостей
- **Модификаторы силы:** зеленый (положительные), желтый (нейтральные), красный (отрицательные)
- **Способности:** синие теги с закругленными углами

## Глобальные функции

Добавлены в `window`:
- `showFieldPlayerHint`
- `calculatePlayerStrength`

## Совместимость

- ✅ Работает с существующей системой составов
- ✅ Интегрируется с системой футболок
- ✅ Совместимо с предыдущими этапами подсказок
- ✅ Поддерживает все формации и позиции

## Планы развития

### ЭТАП 5 (будущее)
- 🔄 Сравнение игроков между командами
- 📊 Статистика игрока в подсказках
- 🎯 Рекомендации по замене игроков
- 📈 График изменения силы по матчам

---

**Дата реализации:** 6 января 2026  
**Версия:** v0.926  
**Статус:** Готово к использованию