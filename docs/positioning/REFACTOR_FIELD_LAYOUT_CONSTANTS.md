# Рефакторинг: Константы для позиционирования поля

## Проблема

В коде было множество **магических чисел** для позиционирования футболок:
- `34px` - отступы контейнера (встречалось в 5+ местах)
- `332px` - ширина рабочей области
- `498px` - высота рабочей области
- `40px` - ширина футболки
- `34px` - высота футболки
- `20px` - половина ширины (для центрирования)
- `17px` - половина высоты (для центрирования)

**Проблемы:**
1. ❌ Дублирование значений по всему коду
2. ❌ Непонятно, откуда взялись числа (особенно 34px)
3. ❌ Сложно изменить размеры - нужно искать все вхождения
4. ❌ Риск ошибок при изменении

## Решение

Создана централизованная константа `FIELD_LAYOUT`:

```javascript
const FIELD_LAYOUT = {
    // Размеры внешнего контейнера (фон поля)
    FIELD_WIDTH: 400,
    FIELD_HEIGHT: 566,
    
    // Отступы контейнера футболок от краёв фона
    // Это значение определяет, насколько внутренний контейнер меньше внешнего
    CONTAINER_PADDING: 34,  // ← Выбрано эмпирически для визуального отступа
    
    // Размеры рабочей области (вычисляются автоматически)
    get WORKING_WIDTH() { return this.FIELD_WIDTH - this.CONTAINER_PADDING * 2; },   // 332px
    get WORKING_HEIGHT() { return this.FIELD_HEIGHT - this.CONTAINER_PADDING * 2; }, // 498px
    
    // Размеры футболок
    SHIRT_WIDTH: 40,
    SHIRT_HEIGHT: 34,
    
    // Половинные размеры (для центрирования)
    get SHIRT_HALF_WIDTH() { return this.SHIRT_WIDTH / 2; },   // 20px
    get SHIRT_HALF_HEIGHT() { return this.SHIRT_HEIGHT / 2; }  // 17px
};
```

## Изменённые функции

### 1. `generateFieldPositionsWithFlankPreservation()`

**Было:**
```javascript
const fieldWidth = 332;
const fieldHeight = 498;
// ...
const SHIRT_HALF_WIDTH = 20;
const SHIRT_HALF_HEIGHT = 17;
```

**Стало:**
```javascript
const fieldWidth = FIELD_LAYOUT.WORKING_WIDTH;   // 332px
const fieldHeight = FIELD_LAYOUT.WORKING_HEIGHT; // 498px
// ...
const SHIRT_HALF_WIDTH = FIELD_LAYOUT.SHIRT_HALF_WIDTH;   // 20px
const SHIRT_HALF_HEIGHT = FIELD_LAYOUT.SHIRT_HALF_HEIGHT; // 17px
```

### 2. `generateFieldPositions()`

**Было:**
```javascript
const fieldWidth = 332;
const fieldHeight = 498;
// ...
const SHIRT_HALF_WIDTH = 20;
const SHIRT_HALF_HEIGHT = 17;
```

**Стало:**
```javascript
const fieldWidth = FIELD_LAYOUT.WORKING_WIDTH;
const fieldHeight = FIELD_LAYOUT.WORKING_HEIGHT;
// ...
const SHIRT_HALF_WIDTH = FIELD_LAYOUT.SHIRT_HALF_WIDTH;
const SHIRT_HALF_HEIGHT = FIELD_LAYOUT.SHIRT_HALF_HEIGHT;
```

### 3. `displayShirtsOnField()`

**Было:**
```javascript
shirtsContainer.style.cssText = 
    'position: absolute; top: 34px; left: 34px; right: 34px; bottom: 34px;';
```

**Стало:**
```javascript
const padding = FIELD_LAYOUT.CONTAINER_PADDING;
shirtsContainer.style.cssText = 
    `position: absolute; top: ${padding}px; left: ${padding}px; right: ${padding}px; bottom: ${padding}px;`;
```

### 4. `createShirtElement()`

**Было:**
```javascript
div.style.cssText = `
    width: 40px;
    height: 34px;
    line-height: 34px;
`;
```

**Стало:**
```javascript
div.style.cssText = `
    width: ${FIELD_LAYOUT.SHIRT_WIDTH}px;
    height: ${FIELD_LAYOUT.SHIRT_HEIGHT}px;
    line-height: ${FIELD_LAYOUT.SHIRT_HEIGHT}px;
`;
```

### 5. `window.debugFieldGrid()`

**Было:**
```javascript
grid.style.cssText = `
    top: 34px;
    left: 34px;
    right: 34px;
    bottom: 34px;
`;
```

**Стало:**
```javascript
const padding = FIELD_LAYOUT.CONTAINER_PADDING;
grid.style.cssText = `
    top: ${padding}px;
    left: ${padding}px;
    right: ${padding}px;
    bottom: ${padding}px;
`;
```

## Преимущества

### ✅ Централизация
Все размеры в одном месте - легко найти и изменить

### ✅ Документация
Каждое значение имеет комментарий, объясняющий его назначение

### ✅ Автоматический пересчёт
Изменение `CONTAINER_PADDING` автоматически пересчитывает `WORKING_WIDTH` и `WORKING_HEIGHT`

### ✅ Читаемость
Вместо `34` теперь `FIELD_LAYOUT.CONTAINER_PADDING` - сразу понятно, что это

### ✅ Безопасность
Невозможно случайно использовать разные значения в разных местах

## Ответ на вопрос: Откуда 34px?

**Короткий ответ:** Выбрано эмпирически (на глаз).

**Длинный ответ:**

Значение `CONTAINER_PADDING = 34px` было выбрано при создании системы визуализации поля. Это отступ от краёв фонового изображения поля (400×566px), который:

1. **Создаёт визуальный отступ** от рамки поля на картинке
2. **Предотвращает наложение** футболок на зелёную рамку фона
3. **Обеспечивает эстетичное расположение** иконок

**Это не связано с:**
- ❌ Размерами элементов на изображении
- ❌ Математическими расчётами
- ❌ Стандартами CSS

**Это просто значение, которое хорошо выглядит визуально.**

Теперь, благодаря константе, если нужно изменить отступы (например, на 30px или 40px), достаточно изменить одно значение:

```javascript
CONTAINER_PADDING: 30,  // Было: 34
```

И все расчёты автоматически обновятся!

## Связь размеров

```
Внешний контейнер: 400×566px (FIELD_WIDTH × FIELD_HEIGHT)
    ↓
    Отступы: 34px со всех сторон (CONTAINER_PADDING)
    ↓
    Рабочая область: 332×498px (WORKING_WIDTH × WORKING_HEIGHT)
    ↓
    Футболки: 40×34px (SHIRT_WIDTH × SHIRT_HEIGHT)
    ↓
    Центрирование: ±20px, ±17px (SHIRT_HALF_WIDTH, SHIRT_HALF_HEIGHT)
```

**Формулы:**
- `WORKING_WIDTH = FIELD_WIDTH - CONTAINER_PADDING * 2`
- `WORKING_HEIGHT = FIELD_HEIGHT - CONTAINER_PADDING * 2`
- `SHIRT_HALF_WIDTH = SHIRT_WIDTH / 2`
- `SHIRT_HALF_HEIGHT = SHIRT_HEIGHT / 2`

## Версия

Изменения внесены в версии: **0.931**

## Файлы изменены

- `calc.user.js`:
  - Добавлена константа `FIELD_LAYOUT`
  - Обновлены функции: `generateFieldPositionsWithFlankPreservation()`, `generateFieldPositions()`, `displayShirtsOnField()`, `createShirtElement()`, `window.debugFieldGrid()`
  
- `docs/FIELD_POSITIONING_SYSTEM.md`:
  - Добавлен раздел о константе `FIELD_LAYOUT`
  - Обновлены примеры кода
  - Добавлено объяснение происхождения значения 34px

## Заключение

Рефакторинг улучшил:
- 📖 **Читаемость** - понятно, что означает каждое число
- 🔧 **Поддерживаемость** - легко изменить размеры
- 🐛 **Надёжность** - невозможно использовать разные значения
- 📚 **Документированность** - комментарии объясняют назначение

Теперь любой разработчик может понять, откуда взялись размеры и как их изменить!
