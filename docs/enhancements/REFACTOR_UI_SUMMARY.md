# Рефакторинг UI калькулятора - Новый функционал

## Что было реализовано

### 1. Калькулятор в новой вкладке
- ✅ Добавлена кнопка "Открыть калькулятор силы" на странице превью матча
- ✅ Калькулятор теперь открывается в том же окне, но в специальном режиме
- ✅ Добавлена кнопка "← Вернуться к превью матча" в режиме калькулятора

### 2. Кнопка "Загрузить состав"
- ✅ Перенесена на страницу калькулятора рядом с существующими кнопками
- ✅ Использует существующие стили интерфейса калькулятора
- ✅ **Активная кнопка**: если у менеджера есть состав в форме отправки состава
  - Загружает состав из `mng_order.php?order_day=XXX`
- ✅ **Пассивная кнопка**: если у менеджера нет состава
  - Заблокированная с соответствующей подсказкой

### 3. Интеграция с формой отправки состава
- ✅ Автоматическое определение `order_day` из URL страницы (параметр `preview`)
- ✅ Проверка наличия состава по ссылке `https://www.virtualsoccer.ru/mng_order.php?order_day=XXX`
- ✅ Парсинг HTML формы для извлечения:
  - Игроков по позициям
  - Позиций игроков
  - Капитана команды
  - Стиля игры (если сохранен)
- ✅ Применение загруженного состава в калькуляторе

### 4. Система вкладок для команд
- ✅ Интерфейс разделен на вкладки "Хозяева" и "Гости"
- ✅ Каждая команда имеет независимые настройки:
  - Тактические настройки (стиль игры, тактика)
  - Состав игроков (11 позиций)
  - Роли (капитан, исполнители стандартов)
- ✅ Селекторы стандартных положений работают независимо для каждой команды:
  - Штрафные удары
  - Угловые удары
  - Пенальти
- ✅ Исправлена проблема дублирования ID селекторов (2026-02-14):
  - Селекторы создаются напрямую через `createElement()` вместо `innerHTML`
  - Ссылки на элементы сохраняются в объекте `lineupBlock`
  - Функция `updateRoleSelectors()` использует ссылки вместо `getElementById()`
  - Каждая команда имеет свои независимые селекторы

## Техническая реализация

### Новые функции:
- `getOrderDayFromCurrentPage()` - получение дня матча из URL
- `checkLineupExists(orderDay)` - проверка наличия состава с множественными методами валидации
- `loadLineupFromOrder(orderDay)` - загрузка состава из формы с надежным парсингом
- `createCalculatorButton()` - создание кнопки калькулятора на превью
- `createLoadLineupButton()` - создание кнопки загрузки состава в калькуляторе
- `applyLoadedLineup()` - применение загруженного состава
- `createTeamTabContent()` - создание контента вкладки команды с независимыми селекторами

### Рефакторинг кода:
- ✅ Удалены избыточные комментарии из кода
- ✅ Создана подробная документация API в `/docs`
- ✅ Кнопка загрузки состава перенесена в калькулятор
- ✅ Использование существующих стилей интерфейса
- ✅ Исправлено дублирование ID селекторов стандартных положений (2026-02-14):
  - Селекторы создаются через `createElement()` с сохранением ссылок в `lineupBlock`
  - Функция `updateRoleSelectors()` работает с конкретным экземпляром `lineupBlock`
  - Устранена проблема с `getElementById()` возвращающим только первый элемент

### Логика работы:
1. При загрузке страницы проверяется режим (обычный или калькулятор)
2. В обычном режиме показывается только кнопка "Открыть калькулятор силы"
3. В режиме калькулятора показывается полный интерфейс с кнопкой "Загрузить состав"
4. Состав передается через localStorage между режимами
5. Кнопка загрузки состава интегрирована в существующий интерфейс калькулятора

## Документация
- ✅ `docs/LINEUP_FUNCTIONS_API.md` - API функций проверки и загрузки состава
- ✅ `docs/SHIRTS_SYSTEM_API.md` - API системы отображения футболок
- ✅ Подробное описание всех функций и алгоритмов

## Совместимость
- ✅ Сохранена полная обратная совместимость
- ✅ Все существующие функции калькулятора работают без изменений
- ✅ Поддержка всех доменов: virtualsoccer.ru, vfleague.com, vfliga.ru, vfliga.com
- ✅ Работает с различными форматами HTML форм отправки состава

## Версия
Обновлена с 0.924 до 0.925

## Статус
🟢 **Готово к тестированию**

Выполнен рефакторинг кода с созданием подробной документации и улучшением пользовательского интерфейса. Кнопка загрузки состава перенесена в калькулятор и использует существующие стили.