# Исправление: Извлечение order_day из URL

## Проблема

Функция `getOrderDayFromCurrentPage()` не могла извлечь день матча из URL типа:
```
https://www.virtualsoccer.ru/previewmatch.php?day=25324&match_id=221390
```

**Симптомы:**
- Логи показывали "Order Day: НЕ ОПРЕДЕЛЕН"
- Кнопка "Загрузить состав" была неактивной
- Функция искала параметры `preview` и `order_day`, но в URL был `day`

## Причина

Функция была написана для поиска параметров `preview` и `order_day`, но в реальных URL Virtual Soccer используется параметр `day` как основной идентификатор дня матча.

## Исправление

### Было:
```javascript
function getOrderDayFromCurrentPage() {
    const urlParams = new URLSearchParams(window.location.search);
    const preview = urlParams.get('preview');
    const orderDay = urlParams.get('order_day');
    
    const result = preview || orderDay;
    return result;
}
```

### Стало:
```javascript
function getOrderDayFromCurrentPage() {
    console.log('🔍 [OrderDay] Извлечение order_day из URL');
    console.log('🌐 Текущий URL:', window.location.href);
    
    const urlParams = new URLSearchParams(window.location.search);
    
    // Проверяем различные возможные параметры
    const day = urlParams.get('day');           // основной параметр в previewmatch.php
    const preview = urlParams.get('preview');   // альтернативный параметр
    const orderDay = urlParams.get('order_day'); // прямой параметр
    const matchId = urlParams.get('match_id');   // для контекста
    
    console.log('📋 URL параметры:', {
        day: day || 'не найден',
        preview: preview || 'не найден', 
        order_day: orderDay || 'не найден',
        match_id: matchId || 'не найден'
    });
    
    // Приоритет: day > preview > order_day
    const result = day || preview || orderDay;
    
    console.log('📅 Итоговый Order Day:', result || 'НЕ ОПРЕДЕЛЕН');
    console.log('🔍 Источник значения:', 
        day ? 'параметр day' : 
        preview ? 'параметр preview' : 
        orderDay ? 'параметр order_day' : 
        'не найден'
    );
    
    return result;
}
```

## Улучшения

1. **Поддержка параметра `day`** - теперь функция корректно извлекает день матча из стандартных URL
2. **Приоритетность параметров** - `day` > `preview` > `order_day`
3. **Улучшенное логирование** - показывает все параметры URL и источник найденного значения
4. **Обратная совместимость** - сохранена поддержка старых параметров

## Ожидаемый результат

После исправления для URL `https://www.virtualsoccer.ru/previewmatch.php?day=25324&match_id=221390`:

```
🔍 [OrderDay] Извлечение order_day из URL
🌐 Текущий URL: https://www.virtualsoccer.ru/previewmatch.php?day=25324&match_id=221390
📋 URL параметры: {
  day: "25324",
  preview: "не найден", 
  order_day: "не найден",
  match_id: "221390"
}
📅 Итоговый Order Day: 25324
🔍 Источник значения: параметр day
```

## Тестирование

Функция теперь корректно работает с различными форматами URL:
- ✅ `?day=25324&match_id=221390` (основной случай)
- ✅ `?preview=25324` (альтернативный)
- ✅ `?order_day=25324` (прямой)
- ✅ Комбинации параметров с правильным приоритетом

## Файлы изменены

- `calc.user.js` - исправлена функция `getOrderDayFromCurrentPage()`
- `docs/LINEUP_FUNCTIONS_API.md` - обновлена документация
- `docs/BUGFIX_ORDER_DAY_EXTRACTION.md` - данный отчет

**Статус:** ✅ Исправлено и готово к тестированию