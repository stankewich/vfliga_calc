# План имплементации системы Chemistry

**Дата**: 25 января 2026  
**Версия**: 0.934  
**Статус**: 📋 Готов к имплементации

## Основа: Правила игры

Система chemistry основана на официальных правилах игры (раздел 2.12):

- Модификатор линии между двумя игроками зависит от их стилей и национальностей
- Модификатор игрока = среднее арифметическое модификаторов всех его линий
- Диапазон: от -5% до +12.5%

## Архитектура решения

### 1. Матрица связей

Используем функцию `getPositionConnections(position, lineup)`:

**Позиции в коде:**
- Защитники: LD, LB, CD, SW, RD, RB
- Полузащитники: LM, LW, CM, DM, AM, FR, RM, RW
- Нападающие: LF, CF, RF, ST

**Особенность GK:** Динамические связи с 4 правилами:
1. LB/RB + SW → только SW
2. 5 защитников с 3+ CD → только CD
3. SW без LB/RB → все кроме CD
4. Остальные случаи → все защитники

```javascript
// Пример для GK
const gkConnections = getPositionConnections('GK', lineup);
// Результат: ['LD', 'CD', 'CD', 'RD'] или ['SW'] или ['CD', 'CD', 'CD']

// Пример для CD
const cdConnections = getPositionConnections('CD', lineup);
// Результат: ['GK', 'LD', 'RD', 'CM', 'DM']
```

**Важно:** 
- Функция возвращает простой массив позиций
- Для GK нужно передавать lineup для определения защитников
- TODO: Возможна доработка связей для других позиций

### 2. Структура данных игрока

Необходимые поля для расчета chemistry:

```javascript
{
    id: 12345,
    name: "Иванов",
    position: 'DC',
    nationality: 'RUS',           // Национальность
    favoriteStyle: 'norm',        // Любимый стиль (norm, sp, bb, brazil, tiki, kat, brit)
    styleKnowledge: 100,          // Изученность стиля (0-100%)
    isLeaguePlayer: false         // Игрок от Лиги
}
```

### 3. Конфиг коллизий стилей (уже существует)

```javascript
const collision_bonuses = {
    norm: null,
    sp: { brit: 0.38 },
    bb: { sp: 0.42 },
    brazil: { bb: 0.34 },
    tiki: { kat: 0.36 },
    kat: { brazil: 0.44 },
    brit: { tiki: 0.40 }
};
```

### 4. Функция проверки коллизии стилей

```javascript
/**
 * Проверяет есть ли коллизия между двумя стилями
 * @param {string} style1 - Стиль первого игрока
 * @param {string} style2 - Стиль второго игрока
 * @returns {boolean} - true если стили в коллизии
 */
function areStylesInCollision(style1, style2) {
    // Проверяем есть ли победа style1 над style2
    const style1Wins = collision_bonuses[style1];
    const style1BeatsStyle2 = !!(style1Wins && style1Wins[style2]);
    
    // Проверяем есть ли победа style2 над style1
    const style2Wins = collision_bonuses[style2];
    const style2BeatsStyle1 = !!(style2Wins && style2Wins[style1]);
    
    // Коллизия есть если один стиль побеждает другой
    return style1BeatsStyle2 || style2BeatsStyle1;
}
```

**Примеры:**
- `areStylesInCollision('sp', 'brit')` → `true` (sp побеждает brit)
- `areStylesInCollision('brit', 'sp')` → `true` (sp побеждает brit, коллизия есть)
- `areStylesInCollision('norm', 'sp')` → `false` (norm нейтрален)
- `areStylesInCollision('sp', 'bb')` → `true` (bb побеждает sp)

### 5. Функция расчета модификатора линии

```javascript
/**
 * Рассчитывает модификатор линии между двумя игроками
 * @param {Object} player1 - Первый игрок
 * @param {Object} player2 - Второй игрок
 * @returns {number} - Модификатор линии от -0.05 до +0.125
 */
function calculateLineModifier(player1, player2) {
    // 1. Проверка на игрока от Лиги
    if (player1.isLeaguePlayer || player2.isLeaguePlayer) {
        return 0;
    }
    
    // 2. Проверка на коллизию стилей (приоритет!)
    if (areStylesInCollision(player1.favoriteStyle, player2.favoriteStyle)) {
        return -0.05; // -5%
    }
    
    // 3. Проверка на совпадение стилей
    if (player1.favoriteStyle === player2.favoriteStyle) {
        // Берем минимальную изученность из двух игроков
        const minKnowledge = Math.min(
            player1.styleKnowledge || 0,
            player2.styleKnowledge || 0
        );
        
        // Таблица бонусов по изученности (из правил игры)
        if (minKnowledge >= 100) return 0.125;  // 12.5%
        if (minKnowledge >= 80) return 0.10;    // 10.0%
        if (minKnowledge >= 60) return 0.075;   // 7.5%
        if (minKnowledge >= 40) return 0.05;    // 5.0%
        if (minKnowledge >= 20) return 0.025;   // 2.5%
        // Если < 20% - стиль не изучен, переходим к проверке национальностей
    }
    
    // 4. Проверка на совпадение национальностей
    if (player1.nationality === player2.nationality) {
        return 0.05; // минимум 5%
    }
    
    // 5. Все остальные случаи (разные нац, разные стили без коллизии)
    return 0;
}
```

**Логика приоритетов:**
1. Игрок от Лиги → 0%
2. Коллизия стилей → -5%
3. Совпадение стилей → от 2.5% до 12.5% (по изученности)
4. Совпадение национальностей → 5%
5. Остальное → 0%

### 6. Функция расчета модификатора игрока

```javascript
/**
 * Рассчитывает модификатор взаимопонимания для игрока
 * @param {Object} player - Игрок
 * @param {Array} lineup - Состав команды (массив объектов игроков)
 * @returns {number} - Модификатор от -0.05 до +0.125
 */
function calculatePlayerChemistryModifier(player, lineup) {
    // 1. Получаем связи для позиции игрока (только ПРЯМЫЕ)
    const connections = getPositionConnections(player.position);
    if (!connections || !connections.direct) {
        return 0;
    }
    
    const directConnections = connections.direct;
    
    // 2. Находим всех связанных игроков в составе
    const connectedPlayers = [];
    for (const connectedPos of directConnections) {
        const connectedPlayer = lineup.find(p => p && p.position === connectedPos);
        if (connectedPlayer) {
            connectedPlayers.push(connectedPlayer);
        }
    }
    
    // 3. Если нет связей - возвращаем 0
    if (connectedPlayers.length === 0) {
        return 0;
    }
    
    // 4. Рассчитываем модификатор каждой линии
    let totalModifier = 0;
    const lineModifiers = []; // Для логирования
    
    for (const connectedPlayer of connectedPlayers) {
        const lineModifier = calculateLineModifier(player, connectedPlayer);
        totalModifier += lineModifier;
        lineModifiers.push({
            position: connectedPlayer.position,
            modifier: lineModifier
        });
    }
    
    // 5. Среднее арифметическое (по правилам игры)
    const avgModifier = totalModifier / connectedPlayers.length;
    
    // 6. Применяем лимиты [-5%, +12.5%]
    const finalModifier = Math.max(-0.05, Math.min(0.125, avgModifier));
    
    // Логирование (опционально)
    console.log(`[Chemistry] ${player.name} (${player.position}):`, {
        'Связей': connectedPlayers.length,
        'Линии': lineModifiers,
        'Средний модификатор': (avgModifier * 100).toFixed(1) + '%',
        'Итоговый модификатор': (finalModifier * 100).toFixed(1) + '%'
    });
    
    return finalModifier;
}
```

### 7. Интеграция с getChemistryBonus

```javascript
function getChemistryBonus(player, lineup, teamStyleId) {
    if (!player || !player.position) return 0;
    
    // Рассчитываем модификатор взаимопонимания
    return calculatePlayerChemistryModifier(player, lineup);
}
```

## Примеры расчетов

### Пример 1: Идеальная команда

**Состав 4-4-2:**
- Все игроки: RUS, стиль 'norm', изученность 100%

**DC (центральный защитник):**
```
Связи: GK, DL, DR, MC, CM, DM
Все игроки: RUS + norm + 100%

Модификаторы линий:
- GK: +12.5% (стили совпадают, изученность 100%)
- DL: +12.5%
- DR: +12.5%
- MC: +12.5%
- CM: +12.5%
- DM: +12.5%

Средний модификатор = (12.5 × 6) / 6 = 12.5%
```

### Пример 2: Смешанная команда

**DC (RUS, norm, 100%):**
```
Связи и модификаторы:
- GK (RUS, norm, 100%): +12.5% (стили совпадают, макс изученность)
- DL (UKR, norm, 80%): 0% (разные нац, стили совпадают но не у обоих 100%)
- DR (RUS, sp, 60%): +5% (одна нац, разные стили без коллизии)
- MC (BRA, brit, 100%): 0% (разные нац, разные стили без коллизии)
- CM отсутствует: линия не считается
- DM (RUS, norm, 40%): +5% (стили совпадают, изученность 40%)

Средний модификатор = (12.5 + 0 + 5 + 0 + 5) / 5 = 4.5%
```

### Пример 3: Коллизия стилей

**MC (RUS, sp, 100%):**
```
Связи и модификаторы:
- DC (RUS, brit, 100%): -5% (коллизия: sp побеждает brit)
- ML (RUS, sp, 80%): +10% (стили совпадают, изученность 80%)
- MR (RUS, sp, 100%): +12.5% (стили совпадают, изученность 100%)
- DM (UKR, norm, 100%): 0% (разные нац, norm нейтрален)
- AM отсутствует
- AC (RUS, sp, 60%): +7.5% (стили совпадают, изученность 60%)

Средний модификатор = (-5 + 10 + 12.5 + 0 + 7.5) / 5 = 5%
```

## Необходимые данные

### Откуда брать данные игроков?

**1. Национальность (`nationality`):**
- Извлекается из HTML страницы игрока
- Поле: флаг или текст национальности
- Формат: 'RUS', 'UKR', 'BRA' и т.д.

**2. Любимый стиль (`favoriteStyle`):**
- Извлекается из HTML страницы игрока
- Поле: "Любимый стиль"
- Формат: 'norm', 'sp', 'bb', 'brazil', 'tiki', 'kat', 'brit'

**3. Изученность стиля (`styleKnowledge`):**
- Извлекается из HTML страницы игрока
- Поле: процент изученности стиля
- Формат: число от 0 до 100

**4. Игрок от Лиги (`isLeaguePlayer`):**
- Определяется по признаку в данных игрока
- Возможно по ID команды или специальному флагу

### Где хранить данные?

Расширить существующую структуру игрока:

```javascript
const player = {
    id: 12345,
    name: "Иванов",
    position: 'DC',
    // ... существующие поля ...
    
    // НОВЫЕ ПОЛЯ для chemistry:
    nationality: 'RUS',
    favoriteStyle: 'norm',
    styleKnowledge: 100,
    isLeaguePlayer: false
};
```

## Этапы имплементации

### Этап 1: Добавить функции расчета
- ✅ `areStylesInCollision()` - использует существующий `collision_bonuses`
- ✅ `calculateLineModifier()` - расчет модификатора линии
- ✅ `calculatePlayerChemistryModifier()` - расчет модификатора игрока

### Этап 2: Обновить getChemistryBonus
- Заменить `return 0` на `return calculatePlayerChemistryModifier(player, lineup)`

### Этап 3: Добавить извлечение данных игроков
- Извлечение nationality из HTML
- Извлечение favoriteStyle из HTML
- Извлечение styleKnowledge из HTML
- Определение isLeaguePlayer

### Этап 4: Тестирование
- Тест на идеальной команде (все +12.5%)
- Тест на смешанной команде
- Тест с коллизиями стилей
- Тест с игроками от Лиги

### Этап 5: Логирование и отладка
- Добавить детальное логирование расчетов
- Визуализация модификаторов линий (опционально)

## Вопросы для уточнения

1. **Откуда брать данные игроков?**
   - Есть ли уже функции извлечения nationality, favoriteStyle, styleKnowledge?
   - Где в HTML находятся эти данные?

2. **Игроки от Лиги:**
   - Как определить что игрок от Лиги?
   - Есть ли признак в данных?

3. **Приоритет правил при конфликте:**
   - Если игроки одной национальности, но стили в коллизии - что важнее?
   - По правилам: коллизия = -5%, но национальность = минимум +5%
   - **Решение:** Коллизия имеет приоритет (проверяется раньше)

4. **Неизвестные стили:**
   - Если стиль не изучен (styleKnowledge = 0) - как обрабатывать?
   - **Решение:** Переходим к проверке национальностей

## Связанные документы

- `CHEMISTRY_SYSTEM_DESIGN.md` - общий дизайн системы
- `SYNERGY_CALCULATION_SYSTEM.md` - система расчета сыгранности (отличается от chemistry!)
