# Устранение проблем с функциями кэша

## Проблема: `clearAllStyleCache is not defined`

### Возможные причины
1. **Скрипт не перезагрузился** после обновления
2. **Ошибка выполнения** при загрузке скрипта
3. **Конфликт с другими скриптами**
4. **Проблема с областью видимости**

## Диагностика

### Шаг 1: Проверьте версию скрипта
```javascript
// В консоли браузера:
console.log('Версия скрипта:', document.querySelector('script[src*="calc.user.js"]')?.src);
```

### Шаг 2: Проверьте загрузку функций
```javascript
// Используйте диагностическую функцию:
cacheTest()
```

**Ожидаемый результат:**
```
🧪 Cache Test v0.942
Functions available: {clearAllStyleCache: "function", getStyleCacheStats: "function"}
✅ clearAllStyleCache доступна
```

### Шаг 3: Полная диагностика
```javascript
testCacheFunctions()
```

## Решения

### Решение 1: Перезагрузка страницы
1. **Обновите страницу** (F5 или Ctrl+R)
2. **Очистите кэш браузера** (Ctrl+Shift+R)
3. **Проверьте функции** снова

### Решение 2: Ручная установка функций
Если функции не загрузились автоматически:

```javascript
// Проверьте, доступны ли функции локально
if (typeof clearAllStyleCache === 'undefined') {
    console.error('Функции кэша не загружены');
} else {
    // Установите вручную
    window.clearAllStyleCache = clearAllStyleCache;
    window.getStyleCacheStats = getStyleCacheStats;
    console.log('✅ Функции установлены вручную');
}
```

### Решение 3: Альтернативные команды
Если основные функции недоступны, используйте альтернативы:

```javascript
// Вместо clearAllStyleCache()
localStorage.removeItem('vs_player_styles_cache');
console.log('Кэш очищен через localStorage');

// Вместо getStyleCacheStats()
const cache = JSON.parse(localStorage.getItem('vs_player_styles_cache') || '{}');
console.log('Кэш:', cache);
```

### Решение 4: Проверка ошибок в консоли
1. Откройте **Developer Tools** (F12)
2. Перейдите на вкладку **Console**
3. Найдите **ошибки** (красные сообщения)
4. Если есть ошибки в `calc.user.js`, сообщите о них

## Временные команды

Пока функции недоступны, используйте эти команды:

### Очистка кэша
```javascript
// Полная очистка
localStorage.removeItem('vs_player_styles_cache');
console.log('✅ Кэш стилей очищен');
```

### Просмотр кэша
```javascript
// Статистика кэша
const cache = JSON.parse(localStorage.getItem('vs_player_styles_cache') || '{}');
console.log('📊 Кэш стилей:', cache);

if (cache.teams) {
    const teams = Object.keys(cache.teams).length;
    const players = Object.values(cache.teams).reduce((sum, team) => 
        sum + Object.keys(team.players || {}).length, 0);
    console.log(`📈 Статистика: ${teams} команд, ${players} игроков`);
}
```

### Очистка конкретной команды
```javascript
// Замените "122" на ID нужной команды
const cache = JSON.parse(localStorage.getItem('vs_player_styles_cache') || '{}');
if (cache.teams && cache.teams['122']) {
    delete cache.teams['122'];
    localStorage.setItem('vs_player_styles_cache', JSON.stringify(cache));
    console.log('✅ Команда 122 удалена из кэша');
}
```

## Проверка после исправления

После применения любого решения проверьте:

```javascript
// Проверка доступности функций
console.log('clearAllStyleCache:', typeof clearAllStyleCache);
console.log('getStyleCacheStats:', typeof getStyleCacheStats);

// Тест функциональности
if (typeof clearAllStyleCache === 'function') {
    console.log('✅ Функции кэша работают');
} else {
    console.log('❌ Функции кэша все еще недоступны');
}
```

## Профилактика

Чтобы избежать проблем в будущем:

1. **Регулярно обновляйте страницу** после изменений в скрипте
2. **Проверяйте консоль** на наличие ошибок
3. **Используйте диагностические функции** для проверки состояния
4. **Сообщайте о проблемах** для их исправления

## Контакты для поддержки

Если проблема не решается:
1. Сохраните **скриншот ошибки** из консоли
2. Укажите **версию браузера** и **операционной системы**
3. Опишите **шаги для воспроизведения** проблемы

---
*Руководство создано: February 3, 2026*
*Версия: 0.942*