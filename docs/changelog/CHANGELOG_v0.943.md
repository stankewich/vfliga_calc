# Changelog v0.943 - Обновление графа связей Chemistry системы

## Дата: February 3, 2026

## Обзор изменений

Реализованы обновления графа связей позиций для Chemistry системы согласно изменениям в `CHEMISTRY_CONNECTIONS_GRAPH.md`.

## Ключевые изменения

### 🔄 Обновлена логика для GK (Вратарь)

#### Новое правило:
**GK связан со ВСЕМИ защитниками в составе**

#### До изменений:
```javascript
// Сложная логика с условиями:
// - Если есть LB/RB + SW → связь только с SW
// - Если 5 защитников с 3+ CD → связь только с CD
// - Если SW без LB/RB → связь со всеми кроме CD
```

#### После изменений:
```javascript
function getGKConnections(lineup) {
    // Простая логика: находим всех защитников в составе
    const defenderPositions = ['LD', 'LB', 'CD', 'SW', 'RD', 'RB'];
    const defenders = [];
    
    for (const position of lineup) {
        if (position && defenderPositions.includes(position)) {
            defenders.push(position);
        }
    }
    
    return defenders; // Все защитники!
}
```

#### Примеры новой логики:
- **Схема 4-4-2**: `GK → LD, CD, CD, RD` (4 связи)
- **Схема 5-3-2**: `GK → LD, CD, CD, CD, RD` (5 связей)
- **Схема 3-5-2**: `GK → LD, CD, RD` (3 связи)

### 🔄 Обновлена логика для LD (Левый защитник)

#### Новое условие:
**LD связан с GK только если количество CD != 3**

#### Реализация:
```javascript
'LD': {
    direct: ['GK', 'CD', 'LM', 'DM'],
    diagonal: ['RD', 'CM', 'LW', 'LF'],
    conditions: {
        'GK': (lineup) => {
            const cdCount = countPositionInLineup(lineup, 'CD');
            return cdCount !== 3; // Связь с GK только если CD != 3
        }
    }
}
```

#### Примеры:
- **Схема 4-4-2** (2 CD): LD связан с GK ✅
- **Схема 5-3-2** (3 CD): LD НЕ связан с GK ❌
- **Схема 3-5-2** (1 CD): LD связан с GK ✅

## Технические изменения

### 1. Полностью переписана функция `getPositionConnections()`

#### Новая структура данных:
```javascript
const connections = {
    'LD': {
        direct: ['GK', 'CD', 'LM', 'DM'],      // Прямые связи
        diagonal: ['RD', 'CM', 'LW', 'LF'],    // Диагональные связи
        conditions: {                           // Условия для связей
            'GK': (lineup) => { /* логика */ }
        }
    }
    // ... остальные позиции
};
```

#### Обработка условий:
```javascript
// Применяем условия если они есть
let directConnections = positionData.direct;
if (positionData.conditions && lineup) {
    directConnections = positionData.direct.filter(connectedPos => {
        const condition = positionData.conditions[connectedPos];
        return !condition || condition(lineup);
    });
}
```

### 2. Упрощена функция `getGKConnections()`

#### Убрана сложная логика:
- ❌ Правило 1: LB/RB + SW → только SW
- ❌ Правило 2: 5 защитников с 3+ CD → только CD
- ❌ Правило 3: SW без LB/RB → все кроме CD
- ❌ Правило 4: Остальные случаи → все защитники

#### Добавлена простая логика:
- ✅ **Единое правило**: GK связан со ВСЕМИ защитниками в составе

### 3. Добавлена вспомогательная функция

```javascript
function countPositionInLineup(lineup, position) {
    if (!lineup) return 0;
    return lineup.filter(pos => pos === position).length;
}
```

### 4. Улучшено логирование

```javascript
console.log(`[CHEMISTRY] GK connections: ${defenders.join(', ')} (${defenders.length} defenders total)`);
console.log(`[CHEMISTRY] ${position} connections: ${directConnections.join(', ')}`);
```

## Влияние на Chemistry расчеты

### ⚠️ ВАЖНО: Максимальный бонус Chemistry всегда 12.5%
Количество связей влияет на **стабильность** расчета, а не на максимальный размер бонуса.

### Изменения в стабильности Chemistry для GK:

#### Схема 4-4-2:
- **До**: Переменное количество связей (1-4)
- **После**: Стабильно 4 связи с защитниками
- **Результат**: Более стабильные и предсказуемые Chemistry расчеты

#### Схема 5-3-2:
- **До**: Могло быть 1-3 связи (зависело от условий)
- **После**: Стабильно 5 связей с защитниками
- **Результат**: Максимальная стабильность Chemistry для GK (больше связей = стабильнее)

#### Схема 3-5-2:
- **До**: 3 связи
- **После**: 3 связи (без изменений)

### Изменения в стабильности Chemistry для LD:

#### Схема 5-3-2 (3 CD):
- **До**: LD связан с GK (больше связей)
- **После**: LD НЕ связан с GK (меньше связей)
- **Результат**: Менее стабильные Chemistry расчеты для LD

#### Остальные схемы:
- **Изменений нет**: LD по-прежнему связан с GK

## Обратная совместимость

### ✅ API совместимость
- Функция `getPositionConnections()` сохраняет тот же интерфейс
- Возвращаемый формат не изменился (массив позиций)

### ⚠️ Изменения в расчетах
- **GK**: Потенциально более стабильные Chemistry расчеты (больше связей)
- **LD в схеме 5-3-2**: Менее стабильные Chemistry расчеты (меньше связей)
- **Остальные позиции**: Без изменений

**Примечание**: Максимальный Chemistry бонус всегда остается 12.5% независимо от количества связей. Больше связей = более стабильные и предсказуемые расчеты.

## Тестирование

### Тестовые сценарии:

#### 1. GK в разных схемах
```javascript
// Схема 4-4-2
const lineup442 = ['GK', 'LD', 'CD', 'CD', 'RD', 'LM', 'CM', 'CM', 'RM', 'CF', 'CF'];
const gkConnections = getPositionConnections('GK', lineup442);
// Ожидается: ['LD', 'CD', 'CD', 'RD'] (4 связи)

// Схема 5-3-2
const lineup532 = ['GK', 'LD', 'CD', 'CD', 'CD', 'RD', 'LM', 'CM', 'RM', 'CF', 'CF'];
const gkConnections = getPositionConnections('GK', lineup532);
// Ожидается: ['LD', 'CD', 'CD', 'CD', 'RD'] (5 связей)
```

#### 2. LD с условием CD count
```javascript
// Схема 5-3-2 (3 CD)
const lineup532 = ['GK', 'LD', 'CD', 'CD', 'CD', 'RD', 'LM', 'CM', 'RM', 'CF', 'CF'];
const ldConnections = getPositionConnections('LD', lineup532);
// Ожидается: ['CD', 'LM', 'DM'] (без GK)

// Схема 4-4-2 (2 CD)
const lineup442 = ['GK', 'LD', 'CD', 'CD', 'RD', 'LM', 'CM', 'CM', 'RM', 'CF', 'CF'];
const ldConnections = getPositionConnections('LD', lineup442);
// Ожидается: ['GK', 'CD', 'LM', 'DM'] (с GK)
```

## Логирование

### Новые логи:
```
[CHEMISTRY] GK connections: LD, CD, CD, RD (4 defenders total)
[CHEMISTRY] LD connections: GK, CD, LM, DM
[CHEMISTRY] LD connections: CD, LM, DM (GK excluded due to CD count = 3)
```

## Документация

### Обновленные файлы:
- ✅ `calc.user.js` - реализация изменений
- ✅ `docs/chemistry/CHEMISTRY_CONNECTIONS_IMPLEMENTATION.md` - анализ и решение
- ✅ `docs/changelog/CHANGELOG_v0.943.md` - документация изменений

### Связанные файлы:
- 📋 `docs/chemistry/CHEMISTRY_CONNECTIONS_GRAPH.md` - источник изменений
- 📋 Функция `calculatePlayerChemistryModifier()` - использует обновленные связи

## Следующие шаги

### Немедленные:
1. ✅ Реализация изменений - ЗАВЕРШЕНО
2. ⏳ Тестирование на реальных данных
3. ⏳ Мониторинг влияния на Chemistry расчеты

### Планируемые:
1. ⏳ Анализ производительности новой логики
2. ⏳ Возможная реализация диагональных связей
3. ⏳ UI индикаторы для визуализации связей
4. ⏳ Тестирование стабильности Chemistry расчетов с новыми связями

---
*Изменения внесены: February 3, 2026*
*Версия: 0.943*
*Статус: ✅ ГОТОВО К ТЕСТИРОВАНИЮ*