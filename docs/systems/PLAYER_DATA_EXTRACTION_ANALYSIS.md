# Анализ извлечения данных игроков

**Дата:** 3 февраля 2026  
**Версия:** 0.936  
**Статус:** 📋 Анализ архитектуры

## Обзор системы

В системе Virtual Soccer Strength Analyzer используется **двухуровневая архитектура** извлечения данных игроков:

1. **Roster данные** - полная информация об игроках команды
2. **Lineup данные** - текущий состав и позиции игроков

## Источники данных

### 1. 🏆 Roster.php - Основной источник данных игроков

**URL:** `${SITE_CONFIG.BASE_URL}/roster.php?num=${teamId}&sort=${sort}`

**Назначение:**
- Загрузка **полных данных всех игроков команды**
- Извлечение характеристик для расчета силы
- Получение данных для Chemistry системы

**Содержимое:**
```javascript
// Содержит JavaScript массив plrdat:
var plrdat = [
    [12345, 1, "Иван", "Иванов", 142, "Россия", "GK", "", 8, 25, 85, 0, 12, 95, 1.05, 89, ...],
    // ... остальные игроки
];
```

**Извлекаемые данные:**
```javascript
{
    id: p[0],                    // ID игрока
    name: `${p[2]} ${p[3]}`,     // Имя и фамилия
    nat_id: p[4],                // ID национальности (для Chemistry)
    nat: p[5],                   // Название национальности (для Chemistry)
    mainPos: p[6],               // Основная позиция
    secondPos: p[7],             // Дополнительная позиция
    age: p[9],                   // Возраст
    baseStrength: p[10],         // Базовая сила
    fatigue: p[12],              // Усталость
    form: p[13],                 // Форма
    form_mod: p[14],             // Модификатор формы
    realStr: p[15],              // Реальная сила
    abilities: `${p[16]} ${p[17]} ${p[18]} ${p[19]}`, // Способности
    real_status: p[31],          // Статус
    real_sign: p[32],            // Подпись
    hidden_style: p[33],         // Скрытый стиль (для Chemistry)
    styleKnowledge: 1.0,         // Изученность стиля (по умолчанию 100%)
    transfer: p[38],             // Трансфер
    training: p[44]              // Тренировка
}
```

**Параметр sort (тип турнира):**
```javascript
const sortMap = {
    friendly: 1,                    // Товарищеские
    preseason_cup: 2,              // Предсезонный кубок
    championship: 3,               // Чемпионат
    national_cup: 4,               // Национальный кубок
    amators: 10,                   // Любители
    challenge_cup: 47,             // Кубок вызова
    champions_league_europe: 8,    // Лига чемпионов Европы
    europa_league: 14,             // Лига Европы
    // ... другие международные турниры
};
```

### 2. 📋 mng_order.php - Текущий состав

**URL:** `${SITE_CONFIG.BASE_URL}/mng_order.php?order_day=${orderDay}`

**Назначение:**
- Получение **текущего состава** игроков
- Определение **позиций в матче**
- Извлечение **ID выбранных игроков**

**Содержимое:**
```html
<!-- HTML селекты с выбранными игроками -->
<select name="plr[0]">
    <option value="12345" selected>Иван Иванов (GK)</option>
</select>
<select name="plr[1]">
    <option value="67890" selected>Петр Петров (LD)</option>
</select>
<!-- ... остальные позиции -->
```

**Извлекаемые данные:**
- Массив ID игроков в составе: `[12345, 67890, ...]`
- Соответствие позиций: `позиция 0 = GK, позиция 1 = LD, ...`

## Архитектура извлечения

### Функция loadTeamRoster()

**Основная функция загрузки данных команды:**

```javascript
function loadTeamRoster(teamId, tournamentType) {
    // 1. Определяем параметр sort по типу турнира
    const sort = sortMap[tournamentType];
    
    // 2. Формируем URL для roster.php
    const url = `${SITE_CONFIG.BASE_URL}/roster.php?num=${teamId}&sort=${sort}`;
    
    // 3. Загружаем HTML страницу
    GM_xmlhttpRequest({
        method: "GET",
        url: url,
        onload: function (response) {
            // 4. Извлекаем plrdat из HTML
            const rawPlayers = extractPlrdatFromHTML(response.responseText);
            
            // 5. Преобразуем в объекты игроков
            const players = extractPlayersFromPlrdat(rawPlayers);
            
            resolve(players);
        }
    });
}
```

### Функция extractPlrdatFromHTML()

**Парсинг JavaScript массива из HTML:**

```javascript
function extractPlrdatFromHTML(html) {
    // Ищем var plrdat = [...]; в HTML
    const match = html.match(/var plrdat\s*=\s*\[(.*?)\];/s);
    if (!match) return [];
    
    const arrText = match[1];
    
    // Парсим массив игроков
    const items = [];
    const playerMatches = arrText.match(/\[([^\]]+)\]/g);
    
    if (playerMatches) {
        playerMatches.forEach(playerStr => {
            // Парсим каждого игрока: [id, team, name, surname, ...]
            const values = playerStr.slice(1, -1).split(',');
            const player = values.map(v => {
                const trimmed = v.trim();
                if (trimmed.startsWith('"') && trimmed.endsWith('"')) {
                    return trimmed.slice(1, -1); // Строка
                }
                return isNaN(trimmed) ? trimmed : Number(trimmed); // Число
            });
            items.push(player);
        });
    }
    
    return items;
}
```

### Функция extractPlayersFromPlrdat()

**Преобразование массива в объекты игроков:**

```javascript
function extractPlayersFromPlrdat(plrdat) {
    return plrdat.map(p => ({
        // Базовые данные
        id: p[0],
        name: `${p[2]} ${p[3]}`,
        mainPos: p[6],
        baseStrength: p[10],
        
        // Chemistry данные (добавлены в v0.933)
        nat_id: p[4],        // ID национальности
        nat: p[5],           // Название национальности
        hidden_style: p[33], // Скрытый стиль
        
        // Style Knowledge (добавлено в v0.936)
        styleKnowledge: 1.0, // По умолчанию 100%
        
        // ... остальные поля
    }));
}
```

## Использование в системе

### 1. Загрузка в калькуляторе силы

```javascript
// В функции init() калькулятора:
const [homePlayers, awayPlayers] = await Promise.all([
    loadTeamRoster(homeTeamId, tournamentType),  // Загружаем roster данные
    loadTeamRoster(awayTeamId, tournamentType)
]);

// Сохраняем для использования в селектах
window.homePlayers = homePlayers;
window.awayPlayers = awayPlayers;
```

### 2. Использование в Chemistry системе

```javascript
// В функции getChemistryBonus():
function getChemistryBonus(player, inLineupPlayers, teamStyleId) {
    // player - объект из loadTeamRoster() с полными данными
    // Содержит: nat_id, nat, hidden_style, styleKnowledge
    
    const modifier = calculatePlayerChemistryModifier(player, inLineupPlayers, positions);
    return modifier;
}
```

### 3. Получение текущего состава

```javascript
// Состав извлекается из слотов калькулятора:
const slotEntries = window.currentSlotEntries || [];

// Каждый entry содержит:
{
    player: { /* объект из roster данных */ },
    matchPos: "GK", // позиция в матче
    // ... другие данные
}
```

## Преимущества архитектуры

### ✅ Полнота данных
- **Roster.php** содержит ВСЕ характеристики игроков
- Включает данные для Chemistry (национальность, стиль)
- Содержит все необходимые поля для расчетов

### ✅ Актуальность по турниру
- Параметр `sort` обеспечивает загрузку данных для конкретного турнира
- Учитывает различия в силе игроков между турнирами
- Поддерживает все типы турниров (чемпионат, кубки, международные)

### ✅ Эффективность
- Один запрос загружает всех игроков команды
- Данные кэшируются в `window.homePlayers` / `window.awayPlayers`
- Повторные обращения не требуют новых запросов

### ✅ Надежность
- Fallback механизм для разных типов турниров
- Обработка ошибок и пустых ответов
- Отладочное логирование для диагностики

## Альтернативные источники

### mng_order.php - НЕ используется для данных игроков

**Почему НЕ используется:**
1. **Ограниченные данные** - содержит только ID и имена игроков
2. **Нет характеристик** - отсутствуют сила, форма, способности
3. **Нет Chemistry данных** - нет национальности и стиля
4. **Доступность** - доступен только для собственной команды менеджера

**Где используется:**
- Определение текущего состава (ID игроков и позиции)
- Проверка наличия состава у менеджера
- Извлечение данных сыгранности (synergy)

### Сравнение источников

| Характеристика | roster.php | mng_order.php |
|----------------|------------|---------------|
| **Полнота данных** | ✅ Все характеристики | ❌ Только ID и имена |
| **Chemistry данные** | ✅ nat_id, nat, hidden_style | ❌ Отсутствуют |
| **Доступность** | ✅ Любая команда | ❌ Только своя команда |
| **Актуальность** | ✅ По типу турнира | ❌ Текущий состав |
| **Использование** | ✅ Основной источник | ❌ Только для составов |

## Потенциальные улучшения

### 1. Кэширование roster данных

```javascript
// Возможное улучшение - кэширование по команде и турниру
const cacheKey = `roster_${teamId}_${tournamentType}`;
if (window.rosterCache && window.rosterCache[cacheKey]) {
    return window.rosterCache[cacheKey];
}
```

### 2. Параллельная загрузка

```javascript
// Текущая реализация уже использует Promise.all:
const [homePlayers, awayPlayers] = await Promise.all([
    loadTeamRoster(homeTeamId, tournamentType),
    loadTeamRoster(awayTeamId, tournamentType)
]);
```

### 3. Извлечение дополнительных данных

```javascript
// Возможно добавить в будущем:
{
    // ... существующие поля ...
    
    // Дополнительные данные для Chemistry:
    realStyleKnowledge: p[XX], // Реальная изученность из plrdat
    teamChemistry: p[YY],      // Командная химия
    
    // Дополнительные характеристики:
    morale: p[ZZ],             // Мораль
    experience: p[AA]          // Опыт
}
```

## Заключение

### Текущая архитектура оптимальна:

1. **✅ roster.php** - единственный источник данных игроков
2. **✅ plrdat массив** - содержит все необходимые характеристики
3. **✅ Chemistry данные** - nat_id, nat, hidden_style извлекаются корректно
4. **✅ Style Knowledge** - реализован как модификатор по умолчанию

### mng_order.php НЕ используется для данных игроков потому что:

1. **Недостаточно данных** - нет характеристик для расчета силы
2. **Нет Chemistry данных** - отсутствуют национальность и стиль
3. **Ограниченная доступность** - только для собственной команды
4. **Неактуальность** - не учитывает тип турнира

### Система работает правильно:
- Все данные игроков извлекаются из **roster.php → plrdat**
- Chemistry система получает корректные данные
- Архитектура обеспечивает полноту и актуальность информации

**Вывод:** Изменения в архитектуре извлечения данных НЕ требуются. Система использует оптимальный источник данных.